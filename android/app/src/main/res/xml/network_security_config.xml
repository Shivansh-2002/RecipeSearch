<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <!-- Add any specific certificates or certificate authorities you want to trust -->
            <!-- For example, if you have a self-signed certificate, you can include it here -->
            <!-- Example:
            <certificates src="resource" />
            -->
        </trust-anchors>
    </base-config>
</network-security-config>